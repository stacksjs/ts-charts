{
  "name": "@ts-charts/dispatch",
  "version": "0.1.1",
  "type": "module",
  "description": "Register named callbacks and call them with arguments.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./src/index.ts"
    }
  },
  "module": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun --bun build.ts",
    "prepublishOnly": "bun --bun run build",
    "test": "bun test"
  }
}
